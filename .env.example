# Studio is local-only by default.
VC_STUDIO_HOST=127.0.0.1
VC_STUDIO_PORT=3100
VC_FIXTURE_PORT=4273

# Use 1 only for automated testing. Interactive Lab Mode is headful.
VC_HEADLESS=0

# The MVP ships a strictly validated mocked AI compiler. No live model call is made.
# A future live compile-time adapter may read this value; runtime never may.
OPENAI_API_KEY=
